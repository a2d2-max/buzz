/**
 * 사람 눈에 보이는 제품 이름. 화면 문구는 전부 여기서 읽는다.
 *
 * 기술 식별자(번들 id, `buzz://` 딥링크, CLI 바이너리 이름, 환경변수,
 * 데이터 폴더, data-testid, Nostr kind/tag)는 상류 "buzz" 이름을 그대로 둔다.
 * Rust/Tauri 설정처럼 TS를 못 읽는 곳은 같은 값을 글자 그대로 적는다.
 */
export const BRAND_NAME = "a2d2";
